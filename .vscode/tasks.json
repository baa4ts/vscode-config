{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🔄 Sincronizar Extensiones del Proyecto",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                "$extensionsPath = Join-Path '${workspaceFolder}' '.vscode\\extensions.json'; if (Test-Path $extensionsPath) { $json = Get-Content $extensionsPath -Raw | ConvertFrom-Json; if ($json.recommendations) { $json.recommendations | ForEach-Object { code --install-extension $_ --force } }; if ($json.unwantedRecommendations) { $json.unwantedRecommendations | ForEach-Object { code --uninstall-extension $_ --force } } } else { Write-Error 'No se encontró el archivo .vscode\\extensions.json' }"
            ],
            "runOptions": {
                "runOn": "folderOpen"
            },
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}
