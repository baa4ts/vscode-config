{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🔄 Sincronizar Extensiones del Proyecto",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                "$json = Get-Content '.vscode\\extensions.json' | ConvertFrom-Json; if ($json.recommendations) { $json.recommendations | ForEach-Object { code --install-extension $_ --force } }; if ($json.unwantedRecommendations) { $json.unwantedRecommendations | ForEach-Object { code --uninstall-extension $_ --force } }"
            ],
            "runOptions": {
                "runOn": "folderOpen"
            },
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": []
        }
    ]
}
